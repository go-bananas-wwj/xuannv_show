# === 玄女底座展示平台 — Python 依赖 ===
# 安装命令: pip install -r requirements.txt
#
# 注意：若目标机器无 GPU，可删除 --extra-index-url 行，
#       pip 会自动安装 CPU 版本 PyTorch。
--extra-index-url https://download.pytorch.org/whl/cu124

# --- Web Framework ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.17
pydantic>=2.9.0
pydantic-settings>=2.0.0

# --- Data & Image Processing ---
numpy>=1.26.0,<2.0
pillow>=11.0.0
rasterio>=1.4.0
pyproj>=3.5.0
matplotlib>=3.9.0

# --- Machine Learning ---
torch>=2.5.0
torchvision>=0.20.0
scikit-learn>=1.5.0
joblib>=1.3.0
tqdm>=4.67.0

# --- SAM3 (Segment Anything 3) ---
einops>=0.8.0
pycocotools>=2.0.0
psutil>=5.9.0
timm>=1.0.17

# --- Utilities ---
requests>=2.30.0

# --- LLM ---
openai>=1.60.0
