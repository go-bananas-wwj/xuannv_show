# 玄女底座展示平台 — 改进计划

> 生成日期: 2026-04-29
> 基于 Phase 1-7 标准化完成后的代码库调研

---

## 一、下游监测能力 — Ground Truth 显示修复

### 1.1 问题诊断

| 任务 | 问题 | 根因 |
|------|------|------|
| **building_extraction** | GT 完全错误，所有像素显示为灰色/非建筑 | 代码期望 GT 值为 `50`（Built-up），但实际数据 `static.tif` 的 unique values 为 `[0, 1]`，映射逻辑不匹配 |
| **jrc_water** | no-data 区域（-128）被错误显示为"非水体" | 代码先设 `-128 → -1`，随后 `<= 0 → 0` 将其覆盖 |
| **dynamic_world** | 可能取错季度文件 | 目录下全是 `2023Q1.tif`、`2024Q2.tif` 等季度文件，代码取 `sorted(glob)[0]`，不一定是期望时间 |
| **worldcover** | 无明显 bug | `static.tif` 标签值与代码逻辑匹配 |

### 1.2 修复方案

**方案 A：修复 GT 映射逻辑（推荐）**

1. `segmentation_engine.py` 中 `building_extraction` 映射改为适配实际数据 `[0, 1]`：
   ```python
   mapped[raw_int == 1] = 1   # 建筑
   mapped[raw_int == 0] = 0   # 非建筑
   ```

2. `jrc_water` 修复 no-data 处理顺序：
   ```python
   mapped[raw_int == -128] = -1   # no-data
   mapped[raw_int == 0] = 0        # 非水体
   mapped[raw_int > 0] = 1         # 水体
   ```

3. `dynamic_world` 增加时间匹配逻辑：根据用户选择的 period（如 `"2025-04"`）匹配最近季度文件。

**方案 B：如果 GT 数据质量确实不行，直接隐藏 GT 列**

在 `segmentation_engine.py` 的 `render_detail_figure` 中，当 `_load_ground_truth` 返回 placeholder 时，detail 图只显示 S2 影像 + 模型预测两列，不显示 GT 列。

> **⚠️ 需要用户确认**：`building_extraction` 的 `static.tif` 中 `0=非建筑, 1=建筑` 这个语义是否正确？以及 `dynamic_world` 是否需要按月份匹配季度 GT 文件？

---

## 二、智能体任务报告模块

### 2.1 现状

- 后端 `agent.py` 完全 Mock，仅返回固定 HTML 模板和常量统计数据
- 前端 UI 已完整（输入框、示例、加载、报告渲染、统计图表）
- `/workspace/xuannv` 外部代码库中**无 Agent 相关代码**可供复用

### 2.2 技术方案对比

| 方案 | 描述 | 优点 | 缺点 | 工作量 |
|------|------|------|------|--------|
| **A. 模板引擎 + 动态数据** | 解析用户 prompt → 提取时间/区域/任务类型 → 调用下游引擎获取结果 → 填充预定义模板 | 可控、无需外部 LLM、响应快 | 不够智能、复杂 query 处理差 | 中等 |
| **B. LLM 驱动（Function Calling）** | 调用 Qwen/GPT API 解析 prompt → 结构化输出任务参数 → 调用下游引擎 → LLM 生成自然语言报告 | 真正智能、用户体验好 | 需要 LLM API Key、有调用成本、延迟较高 | 中等 |
| **C. 混合方案（推荐）** | Prompt 解析用 LLM，报告生成用模板+LLM 润色，数据查询走本地引擎 | 兼顾智能和可控 | 需要 LLM API Key | 中等 |

### 2.3 推荐实现（方案 C）

**后端 Agent 引擎设计：**

```
backend/app/services/agent_engine.py
├── PromptParser（LLM 解析）
│   └── 输入: 用户自然语言 prompt
│   └── 输出: { task_type, region, time_range, focus_classes, output_format }
├── TaskExecutor
│   ├── 变化检测 → 调用 ChangeDetectionEngine
│   ├── 分类统计 → 调用 SegmentationEngine
│   └── 区域概览 → 读取 patches_meta.json
├── ReportGenerator
│   ├── 模板填充（统计数据、结果图 URL）
│   └── LLM 润色（将模板转为流畅中文报告）
└── ImageGenerator
    └── 生成结果专题图（mosaic / 统计图表）
```

**前端改动：**
- `AgentSection.tsx`：增加时间范围选择器（从 prompt 自动提取或手动调整）
- `api.ts`：`submitAgentTask` 增加 `time_range` 参数

> **⚠️ 需要用户确认**：
> 1. 是否有可用的 LLM API Key（如 Qwen、OpenAI、DeepSeek）？当前 `.env` 已预留 `API_KEY` 字段。
> 2. Agent 需要支持哪些任务类型？（变化检测 / 分类统计 / 区域概览 / 自定义？）
> 3. 报告格式：继续用 HTML 字符串，还是改为 Markdown？
> 4. 是否需要异步任务支持（长耗时推理先提交任务 ID，前端轮询结果）？

---

## 三、标注页面 — 现成分类头

### 3.1 现状

- 标注系统与下游检测页面**完全独立**
- `backend/models/` 下已有 4 个预训练 Linear Probe：
  - `worldcover_linear_probe.pkl`
  - `dynamic_world_linear_probe.pkl`
  - `jrc_water_linear_probe.pkl`
  - `building_linear_probe.pkl`
- 当前用户必须从零创建类别、手动标注、训练模型

### 3.2 实现方案

**后端改造：**

1. **新增 "系统模型" 概念**
   - 在 `backend/models/` 下维护 4 个预训练模型（已有）
   - 新增 `SystemModelRegistry`，返回系统模型的元数据（id、name、类别、颜色、描述）
   - 系统模型对所有用户只读共享

2. **扩展 `annotate.py` 路由**
   - `GET /annotate/system-models`：列出 4 个现成分类头
   - `POST /annotate/system-models/{id}/infer`：用系统模型推理
   - `POST /annotate/system-models/{id}/clone`：将系统模型复制到用户模型仓库（用于微调）

3. **推理引擎扩展**
   - `InferenceEngine` 增加 `infer_system_model(model_id, patch_id, month)`
   - 从 `backend/models/` 加载 `.pkl`，推理流程与用户模型相同

**前端改造：**

1. **AnnotatePage 右侧类别面板**
   - 新增 "预置分类头" 区域（折叠面板）
   - 列出 4 个分类头（图标、名称、描述）
   - 点击 "加载类别"：将系统模型的类别和颜色导入到用户 classes 列表
   - 点击 "直接推理"：无需训练，直接用预训练模型对当前 patch 推理
   - 点击 "基于此前训练"：复制系统模型到用户仓库，后续标注作为微调数据

2. **状态管理**
   - `annotateStore` 增加 `systemModels` 列表
   - 加载预置类别时自动设为 activeClassId

> **⚠️ 需要用户确认**：
> 1. 4 个 `.pkl` 模型是否就是下游页面使用的同一批模型？是否需要验证模型准确率？
> 2. "基于预置模型微调"是否有真实需求？（当前 Linear Probe 训练只需秒级，从零训练也很快）
> 3. 预置类别的颜色是否直接采用 `config.json` 中的 `legends` 定义？

---

## 四、标注页面 — 变化检测标注模块

### 4.1 技术可行性

| 评估项 | 难度 | 说明 |
|--------|------|------|
| 后端数据模型扩展 | 低 | Annotation 增加 `period` 字段 |
| 后端 API 修改 | 低 | 增加时间对参数 |
| 前端状态管理扩展 | 低 | 增加 `selectedBeforeMonth`、`selectedAfterMonth` |
| 前端 UI 改造 | **中** | 单画布 → 双期对比视图 |
| SAM3 集成 | 低 | 现有单期分割能力直接复用 |
| **变化检测训练引擎** | **高** | 需重写特征提取逻辑（双期拼接/差值） |

### 4.2 推荐方案：最小可行产品（MVP）

**前端改造：**

1. **双期选择器**：月份下拉框变为两个（Before / After）
2. **对比视图模式**：
   - 默认：左右并排（Before S2 | After S2），中间可拖动分割线
   - 可选：Tab 切换（Before / After / 差值图 / 模型预测图）
3. **标注模式切换**：右侧边栏增加 "变化检测标注" 开关
   - 开启后：类别面板简化为单一"变化区域"类别（或保留细分类别如"新增建筑"、"消失水体"）
   - SAM3 在 After 影像上分割，保存为变化 mask
4. **状态管理**：
   - `annotateStore` 增加 `annotationMode: 'segmentation' | 'change_detection'`

**后端改造：**

1. **Annotation 数据模型扩展**
   ```json
   {
     "id": "ann_xxx",
     "patch_id": "patch_000000",
     "period": "2025-04_vs_2025-10",
     "class_id": "cls_xxx",
     "geometry": {"type": "mask", "mask_b64": "..."}
   }
   ```

2. **SAM3 预加载**
   - `POST /sam/embed` 支持同时预加载 Before + After 两期 embedding

3. **变化检测训练引擎（新增 `ChangeDetectionTrainer`）**
   ```python
   # 最小方案：差值特征 + 二元 LogisticRegression
   emb_before = np.load(f"{patch_id}_{before}.npy")  # [D, 64, 64]
   emb_after = np.load(f"{patch_id}_{after}.npy")    # [D, 64, 64]
   diff = emb_after - emb_before                        # [D, 64, 64]
   # 在变化 mask 区域上训练二元 LR
   ```

4. **变化检测推理引擎**
   - 对任意双期组合计算 `emb_diff` → LR 预测 → 二值变化图 → 热力图可视化

> **⚠️ 需要用户确认**：
> 1. 变化检测标注是否需要"变化类型"分类（新增建筑/消失水体/…），还是只需要"变化/未变化"二分类？
> 2. 是否需要与现有预训练 `ChangeDetectionHeadV3` 集成？（该模型是冻结的 PyTorch 模型，只能做推理不能微调；若需训练只能用轻量 LR）
> 3. 双期对比 UI 偏好：左右并排 vs 叠加切换 vs 滑块对比？
> 4. 变化检测标注的使用场景是什么？（是用于生成训练数据来改进 CD 模型，还是仅做人工标注记录？）

---

## 五、实施优先级与工作量估算

| 优先级 | 需求 | 方案 | 预估工作量 | 依赖 |
|--------|------|------|------------|------|
| **P0** | GT 显示修复 | 修复映射逻辑 | 0.5 天 | 无 |
| **P1** | 现成分类头 | 系统模型注册+推理 | 1.5 天 | 无 |
| **P1** | 智能体报告 | 混合方案（模板+LLM） | 2-3 天 | 需 LLM API Key |
| **P2** | 变化检测标注 | MVP（双期对比+差值LR） | 3-4 天 | 无 |

---

## 六、待确认问题清单

### 必答（影响方案设计）

1. **GT 修复**：`building_extraction` 的 `static.tif` 中 `0=非建筑, 1=建筑` 语义是否正确？
2. **Agent LLM**：是否有可用的 LLM API Key？（Qwen / OpenAI / DeepSeek / 其他？）
3. **Agent 任务范围**：Agent 需要支持哪些任务？（变化检测 / 分类统计 / 区域概览？）
4. **变化检测标注**：需要"变化类型"多分类还是"变化/未变化"二分类？
5. **变化检测标注**：是否需要与预训练 `ChangeDetectionHeadV3` 集成，还是独立训练轻量 LR？

### 可选（影响细节实现）

6. **dynamic_world GT**：是否需要按月份匹配季度文件？
7. **现成分类头**：4 个 `.pkl` 是否就是下游页面使用的同一批模型？
8. **Agent 报告格式**：HTML vs Markdown？
9. **Agent 异步**：长耗时推理是否需要异步任务+轮询机制？
10. **变化检测 UI**：左右并排 vs 叠加切换 vs 滑块对比？
