# 玄女底座展示平台 — 最终改进计划

> 基于用户反馈确认，所有技术方案已确定
> 日期: 2026-04-29

---

## 一、下游监测能力 — Ground Truth 显示修复

### 1.1 修复 jrc_water GT 映射逻辑

**修改文件**: `backend/app/services/segmentation_engine.py`

**具体修改**:
```python
# 修复前（bug: -128 被 <=0 覆盖）
mapped[raw_int == -128] = -1
mapped[raw_int <= 0] = 0
mapped[raw_int > 0] = 1

# 修复后
mapped[raw_int == -128] = -1
mapped[raw_int == 0] = 0
mapped[raw_int > 0] = 1
```

### 1.2 dynamic_world 按月份匹配季度文件

**修改文件**: `backend/app/services/segmentation_engine.py`

**逻辑设计**:
1. `detail` 接口传入的 `period` 参数（如 `"2025-04"`）解析为年份和月份
2. 根据月份确定季度：
   - 01-03 → Q1
   - 04-06 → Q2
   - 07-09 → Q3
   - 10-12 → Q4
3. 在 `RAW_DIR / "dynamic_world" / patch_id` 目录下查找匹配季度的 `.tif` 文件
4. 如果找不到匹配季度，fallback 到 `sorted(glob("*.tif"))[0]`
5. 如果目录为空，返回 placeholder

### 1.3 building_extraction — 从 OSM 重新提取 GT

**用户确认**: 当前 `osm_buildings/static.tif` 数据质量有问题，需要重新提取。

**技术方案**:

使用 `osmnx` 库从 OpenStreetMap 下载建筑物矢量数据，然后栅格化为与 patch 对齐的 TIFF。

**新增脚本**: `scripts/extract_osm_buildings.py`

```python
#!/usr/bin/env python3
"""从 OSM 提取建筑物数据并栅格化为各 patch 的 GT TIFF.

Usage:
    python scripts/extract_osm_buildings.py \
        --region harbin \
        --patches-meta data/harbin/patches_meta.json \
        --output-dir /workspace/raw/harbin_scenes/osm_buildings
"""
```

**实现步骤**:
1. 读取 `patches_meta.json`，获取每个 patch 的 WGS84 bounds
2. 对每个 patch，使用 `osmnx.features_from_bbox()` 下载建筑物 polygon
3. 使用 `rasterio` 创建与 patch 对齐的栅格（256×256，EPSG:32652）
4. 使用 `rasterize` 将建筑物 polygon 烧录为二值 mask（建筑=1，非建筑=0）
5. 保存为 `{patch_id}/static.tif`

**依赖安装**:
```bash
pip install osmnx>=2.0
```

> **注意**: `osmnx` 需要网络访问下载 OSM 数据。哈尔滨区域建筑物数据量约 5-10MB，预计 10-15 分钟完成全部 424 个 patch 的提取。

**修改 segmentation_engine.py**:
```python
# 重新提取后，GT 值为 [0, 1]，修复映射逻辑
mapped[raw_int == 1] = 1   # 建筑
mapped[raw_int == 0] = 0   # 非建筑
# 移除错误的 == 50 判断
```

**工作量**: 0.5 天（不含 OSM 下载时间）

---

## 二、智能体任务报告模块（DeepSeek 驱动）

### 2.1 技术架构

**LLM 配置**:
- Provider: DeepSeek
- 客户端: `openai` Python 库（OpenAI 兼容）
- base_url: `https://api.deepseek.com`
- model: `deepseek-chat`（默认）/ `deepseek-reasoner`（复杂推理）
- API Key: 从 `.env` 的 `DEEPSEEK_API_KEY` 读取
- 输出标识: 每份报告末尾自动添加 `"\n\n---\n*本报告由 DeepSeek AI 生成*"`

**后端模块设计**:

```
backend/app/services/agent/
├── __init__.py
├── config.py              # DeepSeek 客户端初始化
├── prompt_parser.py       # Prompt 解析 → 结构化任务参数
├── task_executor.py       # 任务调度器（调用下游引擎）
├── report_generator.py    # 报告生成（模板 + DeepSeek 润色）
└── image_generator.py     # 结果专题图生成
```

### 2.2 数据模型

**AgentTaskRequest** (扩展):
```json
{
  "prompt": "找出哈尔滨新区 2024 年 3 月到 8 月之间新建的建筑工地",
  "region": "harbin",
  "time_range": ["2024-03", "2024-08"]
}
```

**AgentTaskResponse** (改为 Markdown):
```json
{
  "report_markdown": "# 监测任务报告\n\n## 任务概述\n...",
  "result_image_url": "/api/agent/results/task_xxx.png",
  "statistics": {
    "change_areas": 12,
    "total_area_ha": 45.6,
    "confidence": 0.92,
    "categories": {"construction": 8, "demolition": 3}
  },
  "task_id": "task_xxx"
}
```

### 2.3 Prompt 解析（DeepSeek Function Calling）

**System Prompt**:
```
你是遥感监测智能体助手。请解析用户的自然语言指令，提取以下结构化参数：
- task_type: "change_detection" | "classification_stats" | "region_overview"
- region: 区域名称
- time_range: [开始月份, 结束月份] 或 null
- focus_classes: 关注的地物类别列表（如 ["建筑", "农田"]）
- output_format: "detailed_report" | "summary" | "chart"
```

**Function Schema**:
```python
tools = [{
    "type": "function",
    "function": {
        "name": "parse_monitoring_task",
        "parameters": {
            "type": "object",
            "properties": {
                "task_type": {"enum": ["change_detection", "classification_stats", "region_overview"]},
                "region": {"type": "string"},
                "time_range": {"type": "array", "items": {"type": "string"}},
                "focus_classes": {"type": "array", "items": {"type": "string"}},
                "output_format": {"enum": ["detailed_report", "summary", "chart"]}
            },
            "required": ["task_type", "region"]
        }
    }
}]
```

### 2.4 任务执行器

| 任务类型 | 调用引擎 | 输出 |
|----------|----------|------|
| `change_detection` | `ChangeDetectionEngine` | 变化区域数量、面积、分布图 |
| `classification_stats` | `SegmentationEngine` | 各地物类别面积统计、占比 |
| `region_overview` | `data_loader` + `patches_meta` | 区域基本信息、传感器覆盖、时间范围 |

### 2.5 报告生成器

**模板 + DeepSeek 润色流程**:
1. 任务执行器返回原始数据（JSON）
2. 模板引擎生成结构化 Markdown 草稿：
   ```markdown
   # 监测任务报告

   ## 任务概述
   - 区域: {region}
   - 时间范围: {time_range}
   - 任务类型: {task_type}

   ## 执行结果
   - 检测到变化区域: {change_areas} 处
   - 总面积变化: {total_area_ha} 公顷
   - 主要变化类型: {categories}

   ## 详细分析
   [占位符，由 DeepSeek 润色]

   ---
   *本报告由 DeepSeek AI 生成*
   ```
3. 调用 DeepSeek API 润色"详细分析"段落：
   ```python
   response = client.chat.completions.create(
       model="deepseek-chat",
       messages=[
           {"role": "system", "content": "你是遥感领域专家，请根据以下监测数据撰写专业分析。"},
           {"role": "user", "content": f"数据: {json.dumps(data)}\n请撰写 2-3 段专业分析。"}
       ]
   )
   ```

### 2.6 异步任务支持

**任务状态机**:
```
pending → running → completed / failed
```

**新增路由**:
```python
POST /api/agent/task      # 提交任务，返回 task_id
GET  /api/agent/task/{id} # 查询任务状态和结果
```

**后端实现**:
1. `POST /api/agent/task` 创建 `AgentTask` 记录，状态 `pending`
2. `BackgroundTasks` 异步执行 `_do_agent_task(task_id)`
3. 执行完成后更新状态为 `completed`，存储结果到 `data/agent_results/{task_id}/`
4. 前端轮询 `GET /api/agent/task/{id}`，间隔 2 秒

**前端改动**:
- `AgentSection.tsx`: 提交后显示任务进度条，轮询状态
- 新增 `AgentTaskStatus` 组件：pending → 加载动画 → running → 进度条 → completed → 报告渲染

### 2.7 前端渲染 Markdown

**修改**:
- `AgentSection.tsx` 中 `dangerouslySetInnerHTML` 改为 `react-markdown`
- 安装 `react-markdown` + `remark-gfm`（已依赖，直接使用）

```tsx
import ReactMarkdown from 'react-markdown'
import remarkGfm from 'remark-gfm'

<ReactMarkdown remarkPlugins={[remarkGfm]}>
  {result.report_markdown}
</ReactMarkdown>
```

### 2.8 新增依赖

```bash
pip install openai>=1.0
```

添加到 `requirements.txt` 和 `environment.yml`。

**工作量**: 2.5 天

---

## 三、标注页面 — 现成分类头

### 3.1 后端设计

**系统模型注册表**:

新增 `backend/app/services/system_models.py`:
```python
"""系统预训练分类头注册表."""
from pathlib import Path
import joblib

SYSTEM_MODELS = {
    "worldcover": {
        "name": "WorldCover 土地覆盖",
        "description": "ESA WorldCover 11 类土地覆盖分类",
        "model_path": Path(__file__).resolve().parent.parent / "models" / "worldcover_linear_probe.pkl",
        "classes": [
            {"name": "森林", "color": "#419bdf"},
            {"name": "草地", "color": "#88b053"},
            ...
        ]
    },
    "dynamic_world": { ... },
    "jrc_water": { ... },
    "building_extraction": { ... },
}

def load_system_model(model_id: str):
    meta = SYSTEM_MODELS[model_id]
    model_data = joblib.load(meta["model_path"])
    return model_data, meta["classes"]
```

**新增路由** `backend/app/routers/annotate.py`:
```python
@router.get("/system-models")
async def list_system_models() -> list[dict]:
    """列出可用的系统预训练分类头."""
    ...

@router.post("/system-models/{model_id}/infer")
async def infer_system_model(
    model_id: str,
    patch_id: str,
    month: str,
) -> dict:
    """使用系统预训练模型对指定 patch 推理."""
    ...
```

**推理实现**:
- 复用 `InferenceEngine` 的推理逻辑
- 从 `backend/models/` 加载 `.pkl`
- 颜色映射使用 `config.json` 的 `legends` 定义
- 返回推理结果图 URL

### 3.2 前端设计

**AnnotatePage 右侧边栏改造**:

在"自定义类别"面板上方新增"预置分类头"折叠面板：

```
┌─────────────────────────┐
│  ▼ 预置分类头            │
│  ┌─────────────────────┐│
│  │ 🌍 WorldCover       ││
│  │    土地覆盖分类      ││
│  │    [加载类别] [推理] ││
│  ├─────────────────────┤│
│  │ 🌳 Dynamic World    ││
│  │    土地利用分类      ││
│  │    [加载类别] [推理] ││
│  ├─────────────────────┤│
│  │ 💧 JRC Water        ││
│  │    水体提取          ││
│  │    [加载类别] [推理] ││
│  ├─────────────────────┤│
│  │ 🏢 Building         ││
│  │    建筑物提取        ││
│  │    [加载类别] [推理] ││
│  └─────────────────────┘│
├─────────────────────────┤
│  ▼ 自定义类别            │
│  ...                     │
└─────────────────────────┘
```

**交互逻辑**:
1. **点击"加载类别"**：
   - 调用 `GET /annotate/system-models/{id}` 获取类别列表
   - 将类别添加到用户的 `classes` 列表中
   - 自动设为 `activeClassId`
2. **点击"推理"**：
   - 调用 `POST /annotate/system-models/{id}/infer?patch_id=xxx&month=xxx`
   - 在画布上叠加显示推理结果（与用户自定义模型推理一致）

**API 扩展** `frontend/src/utils/api.ts`:
```typescript
export async function listSystemModels() { ... }
export async function inferSystemModel(modelId: string, patchId: string, month: string) { ... }
```

### 3.3 工作量

- 后端: 0.5 天（SystemModelRegistry + 路由）
- 前端: 1 天（预置分类头 UI + 交互）
- **总计: 1.5 天**

---

## 四、标注页面 — 变化检测标注模块

### 4.1 数据模型扩展

**Annotation 模型扩展**:
```json
{
  "id": "ann_xxx",
  "patch_id": "patch_000000",
  "period": "2025-04_vs_2025-10",
  "class_id": "cls_xxx",
  "geometry": {"type": "mask", "mask_b64": "..."}
}
```

- 新增 `period` 字段（替换单 `month`）
- 保持向后兼容：旧数据 `month` 字段仍然可读

**新增变化检测类别模板**:
```json
{
  "id": "cd_new_building",
  "name": "新增建筑",
  "color": "#ef4444",
  "type": "change_detection"
}
```

预设变化类型类别（用户可增删）:
- 新增建筑
- 消失建筑
- 新增水体
- 消失水体
- 农田转建筑
- 植被消失
- 其他变化

### 4.2 前端改造

#### 4.2.1 状态管理扩展

`frontend/src/stores/annotateStore.ts`:
```typescript
interface AnnotateState {
  // 现有字段...
  annotationMode: 'segmentation' | 'change_detection'
  selectedBeforeMonth: string
  selectedAfterMonth: string
}
```

#### 4.2.2 月份选择器改造

**单期标注模式**（现有）:
```
月份: [2025-04 ▼]
```

**变化检测标注模式**:
```
Before: [2025-04 ▼]    After: [2025-10 ▼]
        [切换为变化检测标注 ▼]
```

#### 4.2.3 画布区域 — 左右并排视图

```
┌──────────────────────────────────────────────────────────┐
│  工具栏: [缩放] [重置] [对比度] [差值图 ▼]                │
├─────────────────────────┬────────────────────────────────┤
│                         │                                │
│   Before S2             │   After S2                     │
│   2025-04               │   2025-10                      │
│                         │                                │
│   ┌─────────────────┐   │   ┌─────────────────┐          │
│   │                 │   │   │                 │          │
│   │   [SAM3 标注区]  │   │   │   [SAM3 标注区]  │          │
│   │                 │   │   │                 │          │
│   └─────────────────┘   │   └─────────────────┘          │
│                         │                                │
│   [← 同步滚动 →]         │                                │
├─────────────────────────┴────────────────────────────────┤
│  Mask 候选: [1] [2] [3]  [保存] [取消]                   │
└──────────────────────────────────────────────────────────┘
```

**交互设计**:
1. 左右两个 Canvas，显示同一 patch 的 Before/After S2 影像
2. 默认在 **After** 影像上做 SAM3 分割（用户标注"变化后区域"）
3. 支持在 Before 影像上标注"变化前区域"（可选）
4. 两个 Canvas 同步缩放和平移
5. 底部显示差值图/模型预测图切换

#### 4.2.4 标注流程

1. 用户选择 Before/After 月份
2. 用户切换到"变化检测标注"模式
3. 系统自动加载两期 SAM3 embedding（`POST /sam/embed` 扩展支持双期）
4. 用户在 After 影像上点击 SAM3 正/负点
5. SAM3 返回 mask 候选
6. 用户选择 mask，为变化类型类别保存
7. 标注保存为 `Annotation(period="2025-04_vs_2025-10")`

#### 4.2.5 新增 API 前端封装

```typescript
// utils/api.ts
export async function preloadSamEmbeddingDual(
  patchId: string, beforeMonth: string, afterMonth: string
) { ... }

export async function saveChangeDetectionAnnotation(
  patchId: string, period: string, classId: string, maskB64: string
) { ... }
```

### 4.3 后端改造

#### 4.3.1 SAM3 预加载扩展

`backend/app/routers/annotate.py`:
```python
@router.post("/sam/embed")
async def preload_sam_embedding(
    patch_id: str,
    month: str,
    before_month: str | None = None,  # 新增
    after_month: str | None = None,   # 新增
) -> dict:
    """预加载 SAM3 embedding. 若传入 before_month + after_month，同时加载两期."""
    ...
```

#### 4.3.2 AnnotationStore 扩展

`backend/app/services/annotate/annotation_store.py`:
- `create_annotation()` 支持 `period` 字段
- mask 文件命名: `{ann_id}_{period}.npz`（避免单期和变化检测冲突）
- `list_annotations()` 按 `period` 过滤

#### 4.3.3 变化检测训练引擎（新增）

`backend/app/services/annotate/cd_trainer.py`:

**训练逻辑**:
```python
class ChangeDetectionTrainer:
    def train(self, user_id: str, model_name: str, period: str) -> dict:
        """基于用户标注的变化检测 mask，训练二元/多类分类器.
        
        特征构造:
          1. 加载两期 embedding: emb_before[D,64,64], emb_after[D,64,64]
          2. 计算特征差值: diff = emb_after - emb_before  # [D,64,64]
          3. 可选: 拼接原始特征: features = cat([emb_before, emb_after, diff])  # [3D,64,64]
          4. 在变化 mask 区域提取正负样本
          
        模型:
          - 二分类（变化/未变化）: LogisticRegression
          - 多分类（变化类型）: LogisticRegression(multi_class='multinomial')
        """
```

**与用户现有 TrainingEngine 的区别**:
| 维度 | 现有分类训练 | 变化检测训练 |
|------|------------|------------|
| 输入 | 单期 embedding [D,64,64] | 双期 embedding [D,64,64] × 2 |
| 特征 | 原始 embedding 像素 | `emb_diff = emb_after - emb_before` |
| 类别 | 用户自定义多类 | 变化类型多类（新增建筑/消失水体/…） |
| 正样本 | 标注区域内的所有像素 | 变化区域内的所有像素 |
| 负样本 | 标注区域外随机采样 | 未变化区域内随机采样 |
| 模型 | LogisticRegression | LogisticRegression（相同） |

#### 4.3.4 变化检测推理引擎（新增）

`backend/app/services/annotate/cd_inference.py`:
```python
class ChangeDetectionInference:
    def infer(self, model_path: str, patch_id: str, period: str) -> Path:
        """对指定 patch 和 period 进行变化检测推理.
        
        1. 加载两期 embedding
        2. 计算 diff = emb_after - emb_before
        3. 加载训练好的 LR 模型
        4. 预测: [64,64] 分类图
        5. 颜色编码为 RGB PNG
        """
```

#### 4.3.5 路由扩展

`backend/app/routers/annotate.py`:
```python
# 变化检测模型训练
@router.post("/cd-models")
async def create_cd_model(...) -> dict:
    """创建变化检测模型并启动训练."""
    ...

# 变化检测推理
@router.post("/cd-models/{id}/infer")
async def infer_cd_model(...) -> dict:
    """使用变化检测模型推理."""
    ...

# 查询变化检测模型
@router.get("/cd-models")
async def list_cd_models(...) -> list[dict]:
    ...
```

### 4.4 工作量估算

| 模块 | 工作量 |
|------|--------|
| 前端双期 UI（左右并排 + 同步交互） | 1.5 天 |
| 前端状态管理扩展 | 0.5 天 |
| 后端 Annotation 模型扩展 | 0.5 天 |
| 后端 SAM3 双期预加载 | 0.5 天 |
| 后端变化检测训练引擎 | 1 天 |
| 后端变化检测推理引擎 | 0.5 天 |
| 后端路由扩展 | 0.5 天 |
| **总计** | **5 天** |

---

## 五、实施优先级与排期

| 优先级 | 需求 | 工作量 | 前置依赖 |
|--------|------|--------|----------|
| **P0** | GT 修复（jrc_water + dynamic_world） | 0.5 天 | 无 |
| **P0** | OSM 建筑物重新提取 | 0.5 天 | 需安装 osmnx |
| **P1** | 现成分类头 | 1.5 天 | 无 |
| **P1** | Agent 智能体报告（DeepSeek） | 2.5 天 | 需 openai 库 |
| **P2** | 变化检测标注 | 5 天 | P1 现成分类头 |

**建议执行顺序**:
1. **Week 1**: GT 修复 + OSM 提取 + 现成分类头（3.5 天）
2. **Week 2**: Agent 智能体报告（2.5 天）
3. **Week 3-4**: 变化检测标注（5 天）

---

## 六、依赖项清单

### 新增 Python 依赖
```
openai>=1.0          # DeepSeek API 客户端（OpenAI 兼容）
osmnx>=2.0           # OSM 建筑物提取（可选，仅在重新提取时安装）
```

### 新增前端依赖
```
# react-markdown 已存在，直接使用
```

### 环境变量
```env
# .env 新增
DEEPSEEK_API_KEY=sk-b37a3a569d1b4299853359e45e0d60ea
```

---

## 七、风险与缓解措施

| 风险 | 影响 | 缓解措施 |
|------|------|----------|
| DeepSeek API 调用失败/限流 | Agent 报告无法生成 | 添加 fallback：模板引擎直接输出，不经过 LLM 润色 |
| OSM 数据下载超时/失败 | building GT 无法重建 | 提供手动上传 GT 的备选方案 |
| 变化检测训练数据不足 | LR 模型效果差 | 增加数据增强（随机翻转/旋转标注 mask） |
| 双期 UI 同步性能差 | 卡顿 | Canvas 使用 requestAnimationFrame + 节流 |
| 变化检测标注与单期标注冲突 | 状态混乱 | 严格区分 `annotationMode`，切换时清空临时状态 |

---

## 八、待用户最终确认

1. **Agent 报告模型选择**: `deepseek-chat`（快、便宜）还是 `deepseek-reasoner`（推理强、慢、贵）？
2. **OSM 提取范围**: 是否只重新提取哈尔滨区域，还是为所有已有区域都提取？
3. **变化检测预设类别**: 上述 7 个预设变化类型是否足够？
4. **Agent 免费额度**: DeepSeek 新用户赠送 10 元额度（约 500 万 token），是否需要设置调用配额限制？
