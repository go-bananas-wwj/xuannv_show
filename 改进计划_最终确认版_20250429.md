# 玄女底座展示平台 — 最终确认版改进计划

> 所有技术方案已根据用户反馈最终确认
> 日期: 2026-04-29

---

## 一、下游监测能力 — Ground Truth 显示修复

### 1.1 jrc_water GT 修复

**修改**: `backend/app/services/segmentation_engine.py`

修复 no-data 被覆盖的 bug：
```python
# 修复前
mapped[raw_int == -128] = -1
mapped[raw_int <= 0] = 0      # bug: 覆盖 -128
mapped[raw_int > 0] = 1

# 修复后
mapped[raw_int == -128] = -1
mapped[raw_int == 0] = 0
mapped[raw_int > 0] = 1
```

### 1.2 dynamic_world 按月份匹配季度

**修改**: `backend/app/services/segmentation_engine.py`

根据 `period` 参数（如 `"2025-04"`）解析季度：
- 01-03 → Q1, 04-06 → Q2, 07-09 → Q3, 10-12 → Q4
- 查找 `dynamic_world/{patch_id}/{year}Q{quarter}.tif`
- 未匹配时 fallback 到 `sorted(glob)[0]`

### 1.3 building_extraction — 从 OSM 重新提取

**范围**: 仅哈尔滨新区（424 个 patches）

**新增脚本**: `scripts/extract_osm_buildings.py`

```python
# 核心流程
for patch in patches:
    bounds_wgs84 = patch["bounds_wgs84"]  # [min_lon, min_lat, max_lon, max_lat]
    # 1. 下载 OSM buildings polygon
    gdf = ox.features_from_bbox(
        bbox=(max_lat, min_lat, min_lon, max_lon),  # north, south, west, east
        tags={"building": True}
    )
    # 2. 转换到 patch 的 UTM CRS (EPSG:32652)
    gdf = gdf.to_crs("EPSG:32652")
    # 3. 栅格化为 256x256 TIFF
    # 使用 rasterio.features.rasterize
    # 建筑像素 = 1, 非建筑 = 0
    # 4. 保存
```

**修改 segmentation_engine.py**: building_extraction 的 GT 映射适配新数据 `[0, 1]`：
```python
mapped[raw_int == 1] = 1   # 建筑
mapped[raw_int == 0] = 0   # 非建筑
```

**依赖**: `osmnx>=2.0`（仅提取脚本使用，不加入 requirements.txt，作为一次性工具安装）

**工作量**: 0.5 天（脚本开发 + GT 映射修复）+ OSM 下载时间（10-15 分钟）

---

## 二、智能体任务报告模块（DeepSeek-V4 驱动）

### 2.1 DeepSeek 模型配置

根据 [DeepSeek 官方定价](https://api-docs.deepseek.com/zh-cn/quick_start/pricing)：

| 场景 | 模型 | 说明 |
|------|------|------|
| Prompt 解析 | `deepseek-v4-flash` | 快、便宜（输入 1元/M tokens，输出 2元/M） |
| 报告润色 | `deepseek-v4-pro` | 质量高（输入 3元/M tokens，输出 6元/M，当前 2.5 折） |

**注意**: `deepseek-chat` 和 `deepseek-reasoner` 已弃用，分别对应 `deepseek-v4-flash` 的非思考与思考模式。

**环境变量**:
```env
DEEPSEEK_API_KEY=sk-b37a3a569d1b4299853359e45e0d60ea
```

**客户端初始化**:
```python
from openai import OpenAI

client = OpenAI(
    api_key=settings.deepseek_api_key,
    base_url="https://api.deepseek.com"
)
```

### 2.2 后端模块设计

```
backend/app/services/agent/
├── __init__.py
├── client.py              # DeepSeek 客户端封装
├── prompt_parser.py       # deepseek-v4-flash Function Calling 解析 prompt
├── task_executor.py       # 任务调度（变化检测 / 分类统计 / 区域概览）
└── report_generator.py    # deepseek-v4-pro 润色报告
```

### 2.3 Prompt 解析流程

**Step 1**: 调用 `deepseek-v4-flash` 解析用户 prompt

```python
response = client.chat.completions.create(
    model="deepseek-v4-flash",
    messages=[
        {"role": "system", "content": "解析遥感监测任务参数"},
        {"role": "user", "content": prompt}
    ],
    tools=[{
        "type": "function",
        "function": {
            "name": "parse_task",
            "parameters": {
                "type": "object",
                "properties": {
                    "task_type": {"enum": ["change_detection", "classification_stats", "region_overview"]},
                    "region": {"type": "string"},
                    "time_range": {"type": "array", "items": {"type": "string"}},
                    "focus_classes": {"type": "array", "items": {"type": "string"}},
                    "output_format": {"enum": ["detailed_report", "summary"]}
                },
                "required": ["task_type", "region"]
            }
        }
    }],
    tool_choice="auto"
)
```

**Step 2**: 任务执行器根据 `task_type` 调用对应引擎获取数据

**Step 3**: 调用 `deepseek-v4-pro` 润色生成 Markdown 报告

```python
report_md = client.chat.completions.create(
    model="deepseek-v4-pro",
    messages=[
        {"role": "system", "content": "你是遥感监测领域专家，请根据数据撰写专业报告。"},
        {"role": "user", "content": f"数据: {json.dumps(data)}\n请生成 Markdown 格式报告。"}
    ]
).choices[0].message.content

# 添加 DeepSeek 标识
report_md += "\n\n---\n*本报告由 DeepSeek AI 生成*"
```

### 2.4 异步任务 + Markdown 输出

**后端路由**:
```python
POST /api/agent/task      → 返回 { task_id }
GET  /api/agent/task/{id} → 返回 { status, report_markdown, statistics, result_image_url }
```

**前端渲染**:
```tsx
import ReactMarkdown from 'react-markdown'
import remarkGfm from 'remark-gfm'

<ReactMarkdown remarkPlugins={[remarkGfm]}>
  {result.report_markdown}
</ReactMarkdown>
```

**新增依赖**: `openai>=1.0`

**工作量**: 2.5 天

---

## 三、标注页面 — 现成分类头

### 3.1 设计

- 4 个预训练模型（`backend/models/*.pkl`）注册为"系统模型"
- 前端右侧边栏新增"预置分类头"折叠面板
- 每个分类头提供两个按钮：
  1. **[加载类别]** — 将系统模型的类别+颜色导入用户 classes 列表
  2. **[直接推理]** — 无需训练，直接用预训练模型对当前 patch 推理

**不支持的**（已确认）: 微调、克隆、基于预置模型训练

### 3.2 工作量

- 后端: `SystemModelRegistry` + 路由 = 0.5 天
- 前端: 预置分类头 UI = 1 天
- **总计: 1.5 天**

---

## 四、标注页面 — 变化检测标注模块

### 4.1 核心设计（用户最终确认版）

**不预设变化类别** — 用户完全自定义，与现有分类头标注机制一致。

**界面模式切换**:
```
标注页面
├─ [常规标注模式]  ← 现有功能
│   └─ 单期影像 + 自定义类别 + SAM3/多边形/折线
│
└─ [变化检测标注模式]  ← 新增
    ├─ Before/After 月份选择器
    ├─ 左右并排双期视图
    ├─ 自定义变化类别面板（与常规类别独立）
    ├─ SAM3 分割（默认在 After 影像上标注变化后区域）
    ├─ 训练变化检测分类头
    └─ 推理展示
```

### 4.2 数据模型

**Annotation 扩展**:
```json
{
  "id": "ann_xxx",
  "patch_id": "patch_000000",
  "period": "2025-04_vs_2025-10",
  "class_id": "cls_xxx",
  "geometry": {"type": "mask", "mask_b64": "..."}
}
```

**变化类别管理**:
- 复用现有的 `ClassManager`，但存储路径隔离：
  - 常规类别: `data/user_annotations/{user_id}/classes.json`
  - 变化类别: `data/user_annotations/{user_id}/cd_classes.json`
- 或共用 `classes.json`，增加 `"type": "change_detection"` 字段区分

### 4.3 前端设计

**模式切换开关**:
```tsx
// 在 AnnotatePage 顶部工具栏
<SegmentedControl
  options={[
    { label: '常规标注', value: 'segmentation' },
    { label: '变化检测标注', value: 'change_detection' }
  ]}
  value={store.annotationMode}
  onChange={(v) => store.setAnnotationMode(v)}
/>
```

**变化检测模式下的 UI 变化**:
1. **月份选择器**: 单下拉框 → Before + After 两个下拉框
2. **画布区域**: 单 Canvas → 左右并排两个 Canvas（Before | After）
3. **类别面板**: 显示变化类别（用户自定义），与常规类别隔离
4. **训练按钮**: 训练"变化检测分类头"（而非常规分类头）
5. **推理结果**: 显示变化检测结果（热力图/二值图）

**双期 Canvas 同步**:
```typescript
// 缩放/平移事件同时作用于两个 Canvas
const handleZoom = (delta: number) => {
  beforeCanvas.zoom(delta)
  afterCanvas.zoom(delta)
}
```

### 4.4 后端设计

**SAM3 双期预加载**:
```python
@router.post("/sam/embed")
async def preload_sam_embedding(
    patch_id: str,
    month: str | None = None,           # 常规标注用
    before_month: str | None = None,    # 变化检测用
    after_month: str | None = None,     # 变化检测用
):
    if before_month and after_month:
        sam.preload_image(patch_id, before_month, "emb_before")
        sam.preload_image(patch_id, after_month, "emb_after")
    else:
        sam.preload_image(patch_id, month, "emb_default")
```

**变化检测训练引擎**:
```python
class ChangeDetectionTrainer:
    def train(self, user_id: str, model_name: str, period: str):
        # 1. 加载两期 embedding
        emb_before = np.load(f"{patch_id}_{before}.npy")   # [D, 64, 64]
        emb_after = np.load(f"{patch_id}_{after}.npy")     # [D, 64, 64]
        
        # 2. 特征构造: 差值 + 拼接
        diff = emb_after - emb_before                        # [D, 64, 64]
        features = np.concatenate([emb_before, emb_after, diff], axis=0)  # [3D, 64, 64]
        
        # 3. 提取训练样本
        # 正样本: 变化 mask 区域内的像素
        # 负样本: 未变化区域内随机采样（上限 = 2×正样本）
        
        # 4. 训练 LogisticRegression
        # 多分类: 不同变化类型（用户自定义类别）
        clf = LogisticRegression(max_iter=1000, multi_class='multinomial')
        clf.fit(X_scaled, y)
        
        # 5. 保存模型
        joblib.dump({"scaler": scaler, "model": clf, "classes": classes, ...}, path)
```

**变化检测推理引擎**:
```python
class ChangeDetectionInference:
    def infer(self, model_path: str, patch_id: str, period: str) -> Path:
        emb_before, emb_after = load_dual_embedding(patch_id, period)
        diff = emb_after - emb_before
        features = np.concatenate([emb_before, emb_after, diff], axis=0)
        
        model_data = joblib.load(model_path)
        pred = model_data["model"].predict(
            model_data["scaler"].transform(features.reshape(3*D, -1).T)
        ).reshape(64, 64)
        
        # 颜色编码 → PNG
        return png_path
```

### 4.5 路由扩展

```python
# 变化检测类别管理（复用现有 ClassManager，路径隔离）
GET  /annotate/cd-classes
POST /annotate/cd-classes
DELETE /annotate/cd-classes/{id}
PATCH /annotate/cd-classes/{id}

# 变化检测标注（复用现有 AnnotationStore，增加 period 字段）
GET  /annotate/cd-annotations?patch_id=xxx&period=xxx
POST /annotate/cd-annotations
DELETE /annotate/cd-annotations/{id}

# 变化检测模型训练
POST /annotate/cd-models
GET  /annotate/cd-models
GET  /annotate/train/{job_id}   # 复用现有训练状态查询

# 变化检测推理
POST /annotate/cd-models/{id}/infer
POST /annotate/cd-models/{id}/infer_batch
```

### 4.6 工作量

| 模块 | 工作量 |
|------|--------|
| 前端模式切换 + 双期 UI | 1.5 天 |
| 前端变化类别管理 | 0.5 天 |
| 后端 Annotation 扩展（period 字段） | 0.5 天 |
| 后端 SAM3 双期预加载 | 0.5 天 |
| 后端变化检测训练引擎 | 1 天 |
| 后端变化检测推理引擎 | 0.5 天 |
| 后端路由扩展 | 0.5 天 |
| **总计** | **5 天** |

---

## 五、实施排期

| 周次 | 任务 | 工作量 |
|------|------|--------|
| **Week 1** | P0: GT 修复 + OSM 提取 + 现成分类头 | 3.5 天 |
| **Week 2** | P1: Agent 智能体报告（DeepSeek-V4） | 2.5 天 |
| **Week 3-4** | P2: 变化检测标注模块 | 5 天 |

---

## 六、依赖项

```bash
# 新增 Python 依赖（加入 requirements.txt）
pip install openai>=1.0

# 一次性工具（不加入 requirements.txt）
pip install osmnx>=2.0

# 前端（已存在，直接使用）
# react-markdown, remark-gfm
```

---

## 七、全部已确认事项清单

| # | 事项 | 确认结果 |
|---|------|----------|
| 1 | building_extraction GT | 从 OSM 重新提取，哈尔滨新区范围 |
| 2 | jrc_water GT | 修复 no-data 处理顺序 |
| 3 | dynamic_world GT | 按月份匹配季度文件 |
| 4 | Agent LLM | DeepSeek-V4（flash 解析 + pro 润色） |
| 5 | Agent API Key | `sk-b37a3a569d1b4299853359e45e0d60ea` |
| 6 | Agent 报告格式 | Markdown，末尾加 DeepSeek 标识 |
| 7 | Agent 异步 | 需要，task_id + 轮询 |
| 8 | Agent 任务范围 | 变化检测统计 / 分类面积统计 / 区域概览 |
| 9 | Agent 配额 | 不加限制 |
| 10 | 现成分类头 | 只提供[加载类别]和[直接推理] |
| 11 | 变化检测类别 | 用户完全自定义（不预设） |
| 12 | 变化检测 UI | 左右并排双期视图 |
| 13 | 变化检测训练 | 差值特征 + LogisticRegression（与现有方式一致） |

---

**等待用户最终确认，确认后立即开始执行。**
