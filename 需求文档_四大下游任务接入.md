# 玄女底座展示平台 — 四大下游任务需求规格文档

## 文档信息
- **版本**: v1.0
- **日期**: 2026-04-18
- **项目**: `/workspace/xuannv_show`
- **状态**: 待开发

---

## 一、背景与目标

### 1.1 当前状态
变化检测功能（`ChangeDetectionHeadV3`）已完全实现并上线：
- ✅ Mosaic tile 独立加载展示
- ✅ Patch 点击弹窗（5图：前期S2 / 后期S2 / 变化前嵌入 / 变化后嵌入 / 变化概率）
- ✅ 后端动态渲染 + LRU 缓存
- ✅ 7 个时间对比期

### 1.2 目标
将 **4 个语义分割/分类下游任务** 接入展示平台，与变化检测并列展示：

| # | 任务 | 数据源 | 类别数 | 类型 |
|---|------|--------|--------|------|
| 1 | WorldCover 土地覆盖分类 | ESA WorldCover | 11 | 多分类 |
| 2 | Dynamic World 土地利用分类 | Google Dynamic World | 9 | 多分类 |
| 3 | JRC 水体提取 | JRC Global Surface Water | 2 | 二分类 |
| 4 | 建筑物提取 | WorldCover Built-up / OSM | 2 | 二分类 |

### 1.3 技术路线决策

**方案对比**:

| 维度 | 方案A: 训练 CNN Head | 方案B: Linear Probe (推荐) |
|------|---------------------|---------------------------|
| 模型复杂度 | 需设计/训练新的 Seg Head | LogisticRegression，零神经网络训练 |
| 开发周期 | 2-3 周（设计 + 训练 + 调参） | 2-3 天（已有 demo_v2 代码可复用） |
| 推理速度 | GPU 上 ~50ms/patch | CPU 上 ~10ms/patch |
| 准确率 | 可能更高（非线性能力） | 已验证：WorldCover BA≈0.85 |
| 依赖 | 需 PyTorch 推理 | sklearn 即可 |
| 部署复杂度 | 高（需 CUDA / 模型文件） | 低（pickle 模型文件） |

**推荐方案 B（Linear Probe）**：
- `demo_v2/components/downstream_tab.py` 中已有完整实现，可直接迁移
- 预训练底座（128维 embedding）+ Linear Probe 已证明有效
- 每个任务模型文件仅 ~500KB（pickle 格式）
- 推理可在 CPU 线程池完成，无需 GPU

---

## 二、任务 1: WorldCover 土地覆盖分类

### 2.1 任务描述
基于预训练 embedding 对遥感影像进行逐像素土地覆盖分类，输出 ESA WorldCover 的 11 个标准类别。

### 2.2 类别定义

| 标签值 | 类别名 (EN) | 类别名 (CN) | 颜色 (RGB) |
|--------|------------|------------|-----------|
| 10 | Tree cover | 森林 | `#419BDF` |
| 20 | Shrubland | 灌木 | `#397D49` |
| 30 | Grassland | 草地 | `#88B053` |
| 40 | Cropland | 农田 | `#FFBB22` |
| 50 | Built-up | 建筑 | `#FFFF4C` |
| 60 | Bare/sparse | 裸地 | `#BB551D` |
| 70 | Snow and ice | 冰雪 | `#DEDEDE` |
| 80 | Water | 水体 | `#AAAAAA` |
| 90 | Wetland | 湿地 | `#785014` |
| 95 | Mangroves | 红树林 | `#8C8C8C` |
| 100 | Moss and lichen | 苔藓 | `#646464` |

### 2.3 数据来源
- **路径**: `/workspace/raw/harbin_scenes/worldcover/{patch_id}/`
- **格式**: GeoTIFF，单波段，uint8 标签值
- **训练样本**: 每个 patch 取最多 300 个像素（随机采样）

### 2.4 模型规格
```python
# 训练脚本参考: demo_v2/components/downstream_tab.py:_render_fullarea_task
from sklearn.linear_model import LogisticRegression
from sklearn.preprocessing import StandardScaler

scaler = StandardScaler()
X_s = scaler.fit_transform(X)  # X: [N, 128] embedding 向量
lr = LogisticRegression(max_iter=2000, class_weight="balanced", solver="lbfgs")
lr.fit(X_s, y)  # y: [N] 标签索引

# 保存
import joblib
joblib.dump({"scaler": scaler, "model": lr, "classes": classes}, "worldcover_linear_probe.pkl")
```

### 2.5 前端展示要求

**Mosaic 展示**:
- 每个 patch 显示分类结果图（128×128，颜色编码）
- 与变化检测相同的 tile 加载架构
- 点击 patch 弹窗显示：原始 S2 影像 + 分类结果 + Ground Truth（如有）

**弹窗布局**:
```
┌─────────────────────────────────────────────────────────┐
│  [原始 S2 影像]    [分类结果]    [Ground Truth]         │
│   256×256          256×256        256×256              │
└─────────────────────────────────────────────────────────┘
```

**图例**:
- 底部显示 11 色图例，带类别名

### 2.6 需要提供的内容
- [ ] 训练好的 `worldcover_linear_probe.pkl` 模型文件
- [ ] 每个 patch 的预计算分类结果图（可选，可动态生成）
- [ ] 验证集指标（Balanced Accuracy、F1-macro）

---

## 三、任务 2: Dynamic World 土地利用分类

### 3.1 任务描述
基于预训练 embedding 进行逐像素土地利用分类，输出 Google Dynamic World 的 9 个类别。

### 3.2 类别定义

| 标签值 | 类别名 (EN) | 类别名 (CN) | 颜色 (RGB) |
|--------|------------|------------|-----------|
| 0 | Water | 水体 | `#0064C8` |
| 1 | Trees | 森林 | `#006400` |
| 2 | Grass | 草地 | `#88B053` |
| 3 | Flooded vegetation | 水淹植被 | `#78B4A0` |
| 4 | Crops | 农田 | `#FFBB22` |
| 5 | Shrub/scrub | 灌木/灌丛 | `#FF9632` |
| 6 | Built | 建筑 | `#FA0000` |
| 7 | Bare | 裸地 | `#B4B4B4` |
| 8 | Snow/Ice | 冰雪 | `#DEDEDE` |

### 3.3 数据来源
- **路径**: `/workspace/raw/harbin_scenes/dynamic_world/{patch_id}/`
- **格式**: GeoTIFF，单波段，uint8 标签值 0-8

### 3.4 模型规格
与 WorldCover 相同，使用 Linear Probe：
```python
lr = LogisticRegression(max_iter=2000, class_weight="balanced", solver="lbfgs")
```

### 3.5 前端展示要求
- Mosaic：9 色分类图
- 弹窗：原始 S2 + 分类结果 + GT
- 图例：9 色图例

### 3.6 需要提供的内容
- [ ] `dynamic_world_linear_probe.pkl` 模型文件
- [ ] 验证指标

---

## 四、任务 3: JRC 水体提取

### 4.1 任务描述
基于预训练 embedding 提取水体区域，二分类问题（水体 / 非水体）。

### 4.2 类别定义

| 标签值 | 类别名 (CN) | 颜色 (RGB) |
|--------|------------|-----------|
| 0 | 非水体 | `#DEDEDE` |
| 1 | 水体 | `#0064C8` |

### 4.3 数据来源
- **路径**: `/workspace/raw/harbin_scenes/jrc_water/{patch_id}/`
- **格式**: GeoTIFF
- **特殊处理**: JRC 数据有 NaN 值（-128），需映射为无标签区域
  ```python
  mapped = np.full_like(raw_int, fill_value=-1)
  mapped[raw_int == -128] = -1      # NaN -> 忽略
  mapped[raw_int <= 0] = 0          # 非水体
  mapped[raw_int > 0] = 1           # 水体
  ```

### 4.4 模型规格
```python
lr = LogisticRegression(max_iter=2000, class_weight="balanced", solver="lbfgs")
# 二分类，使用 binary average for F1
```

### 4.5 前端展示要求
- Mosaic：水体为蓝色，非水体为灰色
- 弹窗：原始 S2 + 水体提取结果 + GT
- 指标展示：Balanced Accuracy、F1-binary

### 4.6 需要提供的内容
- [ ] `jrc_water_linear_probe.pkl` 模型文件
- [ ] 验证指标

---

## 五、任务 4: 建筑物提取

### 5.1 任务描述
基于预训练 embedding 提取建筑物区域，二分类问题（建筑 / 非建筑）。

### 5.2 类别定义

| 标签值 | 类别名 (CN) | 颜色 (RGB) |
|--------|------------|-----------|
| 0 | 非建筑 | `#DEDEDE` |
| 1 | 建筑 | `#FA0000` |

### 5.3 数据来源
**方案 A（推荐）**: 从 WorldCover 提取 Built-up 类
- **路径**: `/workspace/raw/harbin_scenes/worldcover/{patch_id}/`
- **处理**: 标签值 50 → 1（建筑），其他 → 0（非建筑）

**方案 B**: OSM Buildings（如有）
- **路径**: `/workspace/raw/harbin_scenes/osm_buildings/{patch_id}/`

### 5.4 模型规格
与 JRC Water 相同，二分类 Linear Probe。

### 5.5 前端展示要求
- Mosaic：建筑为红色，非建筑为灰色
- 弹窗：原始 S2 + 建筑提取结果 + GT
- 可叠加建筑边界轮廓

### 5.6 需要提供的内容
- [ ] `building_extraction_linear_probe.pkl` 模型文件
- [ ] 验证指标

---

## 六、后端 API 设计

### 6.1 扩展现有 Heads API

现有 `/api/heads/{head_id}/...` 路由已支持 5 个 head_id，需要为新增 4 个任务实现：

```
GET /api/heads/{head_id}/patch/{patch_id}/tile?period={month}
GET /api/heads/{head_id}/patch/{patch_id}/detail?period={month}
GET /api/heads/{head_id}/available-months
```

**注意**: 分类任务是**单期**的（不需要 before/after），所以：
- `period` 参数应为单月份，如 `2025-04`
- 路由逻辑需区分 change_detection（双期）vs 分类任务（单期）

### 6.2 新增服务模块

```
backend/app/services/
  ├── task_engine.py          # 已有: change_detection
  ├── segmentation_engine.py   # 新增: 4 个分类任务的推理引擎
  └── linear_probe_models/     # 新增: 存放 .pkl 模型文件
      ├── worldcover_linear_probe.pkl
      ├── dynamic_world_linear_probe.pkl
      ├── jrc_water_linear_probe.pkl
      └── building_linear_probe.pkl
```

### 6.3 预计算策略

与 change_detection 一致，可选两种方案：

| 方案 | 说明 | 适用场景 |
|------|------|---------|
| 预计算 | 为所有 patch × 所有月份生成 tile + detail | 上线后稳定运行 |
| 动态生成 | 请求时实时推理，加 LRU 缓存 | 开发期、快速迭代 |

**推荐**: 开发期用动态生成，上线前批量预计算。

---

## 七、前端设计

### 7.1 Task Head 切换器

在 MonitoringSection 顶部添加下拉选择器：

```
┌─────────────────────────────────────────────┐
│  下游任务: [变化检测 ▼]  时间: [2025-04 vs 2025-10 ▼]  │
└─────────────────────────────────────────────┘
```

当选择分类任务时，时间选择器变为单月份：

```
┌─────────────────────────────────────────────┐
│  下游任务: [WorldCover分类 ▼]  时间: [2025-04 ▼]      │
└─────────────────────────────────────────────┘
```

### 7.2 MosaicViewer 适配

分类任务的 MosaicViewer 与变化检测一致：
- 相同的 tile 加载架构（128×128）
- 相同的缩放/拖拽交互
- 相同的 hover 高亮 + tooltip

**新增**: 在 MosaicViewer 底部或侧边添加**颜色图例**。

### 7.3 Detail 弹窗适配

分类任务的 detail 弹窗布局：

```
┌────────────────────────────────────────────────────────────┐
│  Patch_000000  |  2025年4月                                │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  [原始 S2 影像]          [分类结果]          [Ground Truth] │
│   256×256                256×256              256×256     │
│                                                            │
├────────────────────────────────────────────────────────────┤
│  🟦 水体  🟩 森林  🟨 农田  🟥 建筑  ...（图例）          │
│  Balanced Accuracy: 0.85  |  F1-macro: 0.82               │
└────────────────────────────────────────────────────────────┘
```

---

## 八、数据需求清单

### 8.1 需要项目开发提供的数据/模型

| # | 内容 | 格式 | 路径建议 | 优先级 |
|---|------|------|---------|--------|
| 1 | WorldCover Linear Probe 模型 | `.pkl` | `backend/models/worldcover.pkl` | P0 |
| 2 | Dynamic World Linear Probe 模型 | `.pkl` | `backend/models/dynamic_world.pkl` | P0 |
| 3 | JRC Water Linear Probe 模型 | `.pkl` | `backend/models/jrc_water.pkl` | P0 |
| 4 | Building Linear Probe 模型 | `.pkl` | `backend/models/building.pkl` | P0 |
| 5 | 各任务验证集指标 | JSON | `backend/models/metrics.json` | P1 |
| 6 | 预计算 tile 图（可选） | PNG | `results/{task}/tiles/` | P2 |
| 7 | 预计算 detail 图（可选） | PNG | `results/{task}/detail/` | P2 |

### 8.2 训练脚本参考

可直接复用 `demo_v2/components/downstream_tab.py` 中的代码：

```python
# 通用训练函数（已有完整实现）
def _render_fullarea_task(
    version: str,           # 模型版本，如 "v4"
    label_source: str,      # 标签数据源，如 "worldcover"
    class_map: dict,        # {标签值: 类别名}
    color_map: dict,        # {标签值: (R,G,B)}
    task_name: str,         # 任务显示名
    is_binary: bool = False,
    jrc_mode: bool = False,
):
    # 1. 加载 embedding maps
    # 2. 加载标签 tif
    # 3. 每 patch 采样 300 像素
    # 4. 训练 Linear Probe
    # 5. 评估 (train/test split)
    # 6. 逐 patch 预测
    # 7. 拼接大图并可视化
```

### 8.3 模型训练步骤

```bash
# 1. 进入 xuannv 项目
cd /workspace/xuannv

# 2. 启动 Python 环境
python demo_v2/components/downstream_tab.py  # 或提取训练逻辑到新脚本

# 3. 对每个任务调用训练
# 已封装在 _run_worldcover_eval("v4") 等函数中

# 4. 保存模型
# 需修改代码，在训练后保存 scaler + lr 到 .pkl
```

---

## 九、开发排期建议

| 阶段 | 任务 | 工作量 | 依赖 |
|------|------|--------|------|
| Phase 1 | 训练 4 个 Linear Probe 模型 | 2-3 天 | embedding 已就绪 |
| Phase 2 | 后端 SegmentationEngine + API 路由 | 2-3 天 | Phase 1 |
| Phase 3 | 前端 Task Head 切换器 + 图例 | 1-2 天 | Phase 2 |
| Phase 4 | Detail 弹窗适配（分类任务布局） | 1-2 天 | Phase 3 |
| Phase 5 | 预计算 + 性能优化 | 1-2 天 | Phase 4 |
| **总计** | | **7-12 天** | |

---

## 十、风险与回退方案

| 风险 | 影响 | 回退方案 |
|------|------|---------|
| Linear Probe 准确率不足 | 展示效果差 | 改用 KNN (demo_v2 已有) 或轻量 CNN Head |
| 标签数据缺失 patch 过多 | 某些 patch 无结果 | 对缺失 patch 显示灰色占位 + "无标签数据"提示 |
| 模型文件过大 | 加载慢 | 使用 sklearn 的 `joblib` + `compress=3` |
| 推理速度不足 | API 响应慢 | 预计算所有结果图，FileResponse 直接返回 |

---

## 十一、附录

### A. 参考代码位置
- **旧 demo 实现**: `/workspace/xuannv/demo_v2/components/downstream_tab.py`
- **模型定义**: `/workspace/xuannv/src/models/heads.py`
- **当前后端引擎**: `/workspace/xuannv_show/backend/app/services/task_engine.py`
- **当前前端展示**: `/workspace/xuannv_show/frontend/src/sections/MonitoringSection.tsx`

### B. 相关数据目录
```
/workspace/raw/harbin_scenes/
├── worldcover/          # ESA WorldCover 11类标签
├── dynamic_world/       # Google Dynamic World 9类标签
├── jrc_water/           # JRC Global Surface Water 水体标签
├── osm_buildings/       # OSM 建筑物标签（备选）
└── s2/                  # Sentinel-2 原始影像
```

### C. Embedding 数据
```
/workspace/outputs/aef_qwen_v4_official/monthly_embeddings_2025/
├── patch_000000_2025-04.npy   # [128, H, W]
├── patch_000000_2025-06.npy
└── ...
```
